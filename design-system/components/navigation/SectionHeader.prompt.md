**SectionHeader** — the tracked uppercase micro-label with an accent tick that heads every row and list section.

```jsx
<SectionHeader title="Daily Puzzles" accent="var(--accent-play)"
  action={<a className="gl-micro">see all →</a>} />
```

Pass `action` for a right-aligned link.
