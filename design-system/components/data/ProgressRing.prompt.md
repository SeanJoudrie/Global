**ProgressRing** — circular completion meter with a monospaced % readout; used for flags learned, codex collection and world-mastery dials.

```jsx
<ProgressRing done={88} total={195} accent="var(--accent-learn)" size={58} stroke={5} />
```

Set `label={false}` for a bare ring. Colour with `accent`.
