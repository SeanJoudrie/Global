**FlagChip** — a flag in a clean white frame, always fully contained (never cropped), with the brand's self-hosted-first fallback chain.

```jsx
<FlagChip code="br" w={88} h={59} />
```

Point `flagBase` at your flag folder (default `assets/flags`). Falls back to flagcdn / jsDelivr, then a glyph placeholder.
